# 安全政策

和生序处理健康记录、图片识别输入和账号身份信息。安全问题优先按可验证事实处理，不在公开仓库中披露个人数据或生产密钥。

## 密钥规则

- API Key、微信 AppSecret、数据库密码和令牌签名密钥只存放在 Azure Secret、CloudBase 控制台或本地未跟踪的 .env。
- 小程序构建变量只能包含公开 HTTPS API/CDN 地址，不能包含 Secret。
- 不要把密钥粘贴到 Issue、Pull Request、聊天、截图、日志或 README。
- 发现密钥疑似泄露时，立即在对应平台撤销并重新生成，再检查 Git 历史和构建产物。

## 数据规则

- 用户健康记录按账号隔离，服务端数据必须经过身份校验。
- 序序相机的原始照片默认只作为临时识别输入，不默认保存。
- AI 返回内容只能作为建议或候选结果，必须经过业务边界检查；食物识别必须由用户确认后保存。
- 删除功能应同时处理数据库记录、关联审计信息和未来可能启用的对象存储文件。

## 报告问题

这是个人项目。请不要公开发布敏感细节；将复现步骤、影响范围和脱敏日志发送给项目维护者，并在报告中说明是否已经撤销相关密钥。
