# ADR-001：采用模块化单体

**状态：** 已接受
**日期：** 2026-08-24

阶段 0 至阶段 2 的服务端采用 NestJS 模块化单体，而不是微服务。当前团队和产品尚未出现独立部署、独立扩缩容或独立所有权需求，过早拆分会增加本地开发、调试、数据一致性和运维成本。

每个领域模块只通过公开 service、领域事件或 `@heban/contracts` 通信。模块不得跨模块直接访问 repository 或 Prisma model。满足明确的独立运行需求后，再用 ADR 评估是否拆分服务。
