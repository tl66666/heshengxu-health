# 如何查看最新改动（傻瓜式指南）

## 问题诊断

你说"看不到变化"，原因是：

1. **微信开发者工具可能打开了错误的目录**
2. **有两个产物目录混淆了**：
   - `apps/mini/dist/build/mp-weixin` ← 我一直在测试这个
   - `apps/mini/dist/dev/mp-weixin` ← 你可能打开的是这个旧的

## 正确操作步骤（5 步）

### 1. 关闭微信开发者工具

完全退出，不要只是最小化

### 2. 清理所有旧产物

```powershell
cd D:\禾伴\heban-ai-health-demo
Remove-Item -Recurse -Force apps\mini\dist\build -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force apps\mini\dist\dev -ErrorAction SilentlyContinue
```

### 3. 重新构建（开发模式）

```powershell
.\scripts\dev-mini.ps1
```

**这个窗口不要关**，保持运行，每次改代码后会自动重新编译。

### 4. 打开微信开发者工具

- 点击"导入项目"
- **项目路径选择**：`D:\禾伴\heban-ai-health-demo\apps\mini\dist\dev\mp-weixin`
  - ⚠️ 注意是 `dist\dev\mp-weixin`，不是 `dist\build\mp-weixin`
- AppID：测试号（或者点"测试号"）

### 5. 看到最新效果

- 欢迎页：全屏插画，底部白色对话气泡
- 步骤页：白色卡片，纯色渐变背景
- BMI 区域：大数字下方有彩色刻度条（蓝绿黄红四色）+ 三角指针

## 如果还是看不到变化

1. **点击微信开发者工具右上角"清缓存" → "全部清除"**
2. **点击"编译" → "重新编译"**
3. 如果还不行，截图给我看：
   - 你的项目路径（开发者工具左上角显示的）
   - 建档页当前的样子

## 我接下来要做什么

我会重新审视建档页设计，按照：

1. **薄荷健康**的沉浸式体验
2. **Apple 官网**的呼吸留白和高级感
3. **我的专业判断**大胆改进

如果现有代码不合理，我会直接重写，不再小修小补。
