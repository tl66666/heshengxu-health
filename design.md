# 和生序 UI 设计规范

版本：v2.0（2026-09-01）
适用范围：`prototypes/web-demo`、`apps/mini` 及后续新增页面。

## 设计方向

“自然纸感 + 克制留白”的健康管理产品。整体气质参考薄荷健康的温暖、可信和手绘插画，同时吸收 Apple 的信息层级、留白和单一主动作原则。页面优先让用户看懂今天发生了什么，再提供下一步行动。

## 配色

| 角色 | 色值 | 用法 |
| --- | --- | --- |
| 页面背景 | `#F5F2EB` | 页面底色、非内容区域 |
| 内容表面 | `#FFFDF9` | 卡片、弹层、输入区 |
| 主色深绿 | `#173F30` | 主标题、关键数字、导航强调；不用于大面积主按钮 |
| 主色绿 | `#1F6B4C` | 链接、选中态、进度、图标 |
| 操作绿 | `#5F9A7D` | 保存、提交等主动作按钮 |
| 辅助绿底 | `#E7F0E9` | 轻提示、标签、选中背景 |
| 辅助蓝 | `#6A98AC` | 睡眠、数据、次级信息 |
| 辅助蓝底 | `#E9F1F4` | 数据提示、用户消息 |
| 辅助暖色 | `#C78A3B` | 轻量提醒、趋势强调，使用面积不超过 10% |
| 主要文字 | `#1E2D25` | 正文、数字、按钮文字 |
| 次要文字 | `#748078` | 说明、时间、辅助标签 |
| 分割线 | `#E2E5DC` | 边框、列表分隔 |

禁止：紫色渐变、荧光色、大面积纯黑；同一屏同时使用超过 3 个强调色。

## 字体

- 中文标题：`Noto Serif SC`, `Songti SC`, `STSong`, serif；用于 H1/H2、品牌名和重要营销标题。
- 正文与控件：`-apple-system`, `BlinkMacSystemFont`, `PingFang SC`, `Microsoft YaHei`, sans-serif。
- H1：28px，字重 700，行高 1.25。
- H2：22px，字重 700，行高 1.35。
- 卡片标题：16px，字重 700，行高 1.4。
- 正文：15px，字重 400，行高 1.6。
- 辅助说明：12px，字重 400，行高 1.5。
- 关键数字：32-40px，字重 700；不要用字距拉伸数字。

移动端使用项目原有 `rpx` 比例换算，保持上述相对层级，不单独缩小标题来塞内容。

## 间距

基础间距单位为 4px，常用值如下：

- 页面左右安全边距：16px（宽屏可扩展到 20px）。
- 卡片内边距：20px；紧凑信息块：14-16px。
- 模块间距：16px；同一模块内标题到内容：8-12px。
- 列表行高：52-56px；底部导航内容区预留 104px。
- 图片与文字之间：12-16px；图标与标签之间：6-8px。
- 不用负 margin 叠压文字或图片，图片必须在自己的可视区域内完整显示。

## 组件样式

### 按钮

- 主按钮：使用 `rgba(237,247,240,.78)` 的浅色玻璃表面，外边框使用同色系 `rgba(159,195,173,.62)`，白色只作为内部顶部高光，避免形成一圈突兀白框；文字 `#2F5F4A`，圆角 14-16px，最小高度 48px，字重 600。按下态提高底色不透明度并轻微缩放至 0.985。
- 语义变体：喝水可使用 `rgba(230,246,249,.82)` + `#3F7180`；经期可使用 `rgba(252,240,244,.84)` + `#765565`。变体只改变色相，不改变材质、尺寸和字阶。
- 次按钮：透明或 `#FFFDF9` 背景，边框 `#E2E5DC`，文字 `#1F6B4C`，圆角 12px。
- 图标按钮：36-42px 正方形，圆角 10-12px；优先使用现有 SVG 图标，图标与文字不重复表达。
- 按下态：整体缩放不超过 0.985，颜色加深，不改变尺寸导致布局跳动。

### 卡片与列表

- 卡片背景 `#FFFDF9`，边框 1px `#E2E5DC`，圆角 16px。
- 阴影：`0 10px 28px rgba(32, 55, 42, 0.08)`，只用于承载独立任务的卡片；列表容器不叠加多重阴影。
- 需要玻璃感时只使用 `rgba(255,255,255,.86-.94)` 的浅透明表面、1px 白色高光边和单层阴影；禁止重模糊、厚描边和多层渐变叠加。
- 信息块可使用 `#E7F0E9`、`#E9F1F4` 或 `#F8F0DF`，不使用渐变填充。
- 卡片内标题、数值、说明按“标题 > 数值 > 说明”纵向层级排列。

### 导航栏

- 底部导航使用半透明 `#FFFDF9`，顶部 1px 分割线，轻微上浮阴影。
- 5 项等宽；中间 AI 入口为 54px 圆形深绿按钮，向上浮出 28px，配合短标签。
- 选中态仅改变主色和字重，不同时放大图标、加粗边框和加背景。

### 图片与图标

- 人物形象（序序）保持原图，不重新绘制造型。
- 横幅图使用 `object-fit: cover` 时必须确认主体不被裁切；人物、产品和文字优先使用 `contain` 或调整 `object-position`。
- 插画不叠加不透明色块，不用 `mix-blend-mode` 造成主体变脏；需要融合时使用相邻背景色和轻阴影。
- 图标统一线性或统一填充风格，尺寸 20-24px（导航图标 22-24px）。

## 布局层级

1. 页面标题与今日上下文。
2. 一个主视觉或核心指标。
3. 关键行动（记录、咨询、开始计划）。
4. 次级数据与可选模块。
5. 底部导航和免责声明。

每个首屏只保留一个最强视觉焦点；不要在同一层级并列多个抢注意力的渐变卡片、彩色按钮或大插画。

### 小程序首页结构

首页固定采用“问候上下文 → 体重方案 → 饮食记录 → 序序相机 → 体重记录 → 四项追踪 → 其他计划”的顺序。体重方案和饮食记录是首屏的两个核心信息块，序序相机作为唯一高强调行动；水、睡眠、运动、心情使用同一尺寸的 2×2 网格，不再各自使用不同颜色的大卡片。

## 新页面检查清单

- 开发前先阅读本文件，并复用已有 CSS 变量。
- 首屏是否只有一个主动作，标题、数值、说明是否形成清晰对比。
- 图片是否完整、清晰，是否遮挡文字或控件。
- 桌面宽度 430px 与移动宽度 375px 下是否无横向溢出、无文字截断。
- 交互态是否只改变颜色、阴影或轻微缩放，不导致布局跳动。

## v2.1 全局落地约束（2026-09-03）

本版本将视觉系统落地到 `apps/mini/src/styles/visual-system.css`，并由 `apps/mini/src/App.vue` 最后加载。后续页面必须优先复用这些变量，不在页面底部追加互相覆盖的“最终样式”。

### 移动端基准

- 以 375px iOS 和 360px Android 为窄屏基准，页面左右内边距为 24px；常规宽度使用 16px，宽屏预览可扩展到 20px。
- 任何固定底部操作都必须预留 `env(safe-area-inset-bottom)`；底部导航内容区至少预留 104px。
- 页面根容器必须 `box-sizing: border-box`、`min-width: 0`、`overflow-x: hidden`，不得依赖负 margin 叠放内容。
- 插画优先使用独立可视区域和 `contain`；横幅使用 `cover` 时必须验证人物、序序和关键物件没有被裁掉。

### v2.1 颜色与材质令牌

| 令牌 | 色值 | 用途 |
| --- | --- | --- |
| `--hz-bg` | `#FFFDF9` | 所有页面主画布 |
| `--hz-surface` | `#FFFFFF` | 卡片、表单、弹层 |
| `--hz-surface-soft` | `#F5F8F3` | 分组底、空状态、轻提示 |
| `--hz-ink` | `#1E2D25` | 标题、关键数值 |
| `--hz-muted` | `#748078` | 正文说明、时间、辅助信息 |
| `--hz-green-deep` | `#173F30` | 强调标题和主导航 |
| `--hz-green` | `#2F6B4D` | 主动作、选中态、进度 |
| `--hz-green-soft` | `#E7F0E9` | 选中背景、标签 |
| `--hz-rule` | `#E2E8E1` | 边框与分割线 |

玻璃效果只允许使用一层 `rgba(255,255,255,.76-.90)`、1px 高光边和单层阴影；同时提供不透明白色降级。禁止在同一组件叠加多层 blur、渐变和重阴影。

### v2.1 组件几何

- 卡片圆角 `22rpx`，紧凑控件圆角 `16rpx`，大幅插画容器圆角 `30rpx`。
- 卡片阴影统一为 `0 10rpx 28rpx rgba(32,55,42,.07)`；浮层使用 `0 18rpx 42rpx rgba(24,52,37,.14)`。
- 主按钮最小高度 `78rpx`，浅绿玻璃表面、绿色文字和 1px 同色边框；按下只缩放到 `0.985`，不得引起布局跳动。
- 图标按钮固定 52-56rpx 盒子，图标用现有 SVG 或匹配的水彩图，不使用 emoji 代替资源。

### 资源约定

- 默认用户头像使用 `/static/illustrations/default-user-avatar.png`，来源为用户提供的图 1；序序对话头像仍使用 `xuxu-avatar.png`。
- 计划页 Hero 使用 `/static/illustrations/plan-hero-journal.png`，来源为用户提供的图 2；图片位于独立上半区，文案位于下方白色信息区，避免文字压住人物和手账。
- 新增图片前先检查 `assets/illustrations` 与 `assets/icon` 是否已有可复用资源；只有缺少关键视觉资产时才调用生图，并保持人物造型一致。

### 新页面门禁

开发新页面前必须阅读本文件，并完成：375px/360px 无横向溢出检查、iOS 安全区检查、图片完整性检查、主要按钮唯一性检查，以及安卓不支持 `backdrop-filter` 时的实色降级检查。
